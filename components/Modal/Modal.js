import React from 'react';
import SelectionModal from './SelectionModal/SelectionModal';

const Modal = () => {
    return (
        <div>
            <SelectionModal />
        </div>
    );
};

export default Modal;
